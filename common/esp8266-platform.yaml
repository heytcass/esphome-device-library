# ESP8266 platform configuration
# Works for all ESP8266 variants

substitutions:
  verify_ssl: "false"  # ESP8266 Arduino has limited TLS support

esp8266:
  # board: set in device file
  framework:
    version: recommended
  restore_from_flash: true
