# Automatically load the Nix development environment
# when entering this directory
use flake

# Optionally set project-specific environment variables
export ESPHOME_PROJECT_DIR="$(pwd)"

# Add local bin to PATH if you have custom scripts
# export PATH="$PWD/bin:$PATH"
