# BLE support for ESP32 variants that have it
# Include this ONLY for: esp32, esp32c3, esp32c6, esp32s3, esp32h2
# Do NOT include for: esp32s2, esp32c2 (no BLE)

esp32_ble_tracker:
  scan_parameters:
    interval: 1100ms
    window: 1100ms
    active: true

bluetooth_proxy:
  active: true
